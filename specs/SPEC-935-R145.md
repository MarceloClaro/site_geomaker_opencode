---
spec_id: SPEC-935-R145
title: "Estabilidade TouchTerrain: hang de threads e auto-recuperação"
component: opt/geomaker/touchterrain, opt/geomaker/watchdog
test_file: tests/test_r145_touchterrain_estabilidade.py
status: green
---

# SPEC-935-R145 — Estabilidade do TouchTerrain (hangs de thread + double-free GDAL)

## Objetivo

Diagnosticar e corrigir a causa raiz de dois defeitos observados no serviço
`geomaker-touchterrain` (gunicorn + Flask + GDAL/Earth Engine): (1) crashes
recorrentes por corrupção de memória (`double free or corruption`) em
threads GDAL/OSGeo, e (2) o endpoint `/main` ficando permanentemente sem
resposta (hang) quando chamadas de rede ao Earth Engine não retornam e
não há timeout configurado nesse nível — consumindo threads do pool do
gunicorn até esgotá-lo. Adicionar uma camada de auto-recuperação (watchdog)
que restaura o serviço automaticamente quando ele para de responder, sem
depender de correção do código vendorizado do TouchTerrain.

## Motivação

Em 16/07/2026, o usuário reportou "HTTP 524" ao usar o site. A investigação
via `journalctl -u geomaker-touchterrain` revelou:

```
09:23:18  gunicorn: Worker (pid:109170) was sent SIGABRT!   (double free or corruption (out))
09:37:28  gunicorn: double free or corruption (top)
10:34:39  googleapiclient.http: Sleeping 1.69s ... after [Errno 32] Broken pipe
10:51:30  gunicorn: double free or corruption (top) → Worker (pid:122194) SIGABRT
```

E, no momento da investigação, `curl --max-time 8 http://localhost:8081/main`
não retornava (timeout), enquanto `http://localhost:8081/` (raiz) respondia
normalmente em <10ms. Inspeção de `/proc/<pid>/task/` mostrou 17–19 threads
por worker gunicorn, muito acima do configurado (`--workers 2 --threads 4`
→ esperado ~4-6 por processo), indicando vazamento/acúmulo de threads
bloqueadas — consistente com chamadas de rede ao Earth Engine sem timeout
que nunca retornam, cada uma prendendo uma thread do pool indefinidamente.

O `--timeout 600` do gunicorn opera no nível de *worker* (heartbeat do
processo com o master), não no nível de *thread individual* — por isso um
worker com uma ou mais threads bloqueadas continua enviando heartbeat
normalmente (via outras threads livres) e nunca é reciclado pelo timeout,
permitindo o acúmulo observado.

O bug `KeyError: 'basethick'` (SPEC-935-R143) já estava corrigido no código
(`args.get(k, 0) or 0`, linha 391); as ocorrências vistas nos logs eram de
*antes* do restart do serviço às 09:10:26 — não é uma regressão ativa, mas
o teste de regressão deve permanecer para evitar reintrodução do bug.

## Escopo

### Em escopo
- Script de watchdog `scripts/touchterrain_watchdog.py`:
  - Testa `GET http://localhost:8081/main` (ou endpoint equivalente leve)
    com timeout curto (ex.: 10s)
  - Se falhar (timeout, conexão recusada, HTTP 5xx), reinicia
    `geomaker-touchterrain` via `systemctl restart` e registra o evento
  - Idempotente e seguro para rodar repetidamente via timer
- Unidade systemd `geomaker-touchterrain-watchdog.timer` +
  `geomaker-touchterrain-watchdog.service` (executa o script periodicamente,
  ex.: a cada 2 minutos)
- Teste de regressão para `KeyError: 'basethick'` (SPEC-935-R143 continua
  válida) confirmando que o código atual usa acesso seguro (`.get`)
- Teste que documenta e detecta (via análise de logs) o padrão de crash
  double-free, permitindo alerta precoce em auditorias futuras
- Verificação pós-correção: `/main` responde (não trava) após restart

### Fora de escopo
- Correção do código C/GDAL vendorizado do TouchTerrain (thread-safety
  interna do OSGeo/GDAL) — mitigado operacionalmente via watchdog, não
  via patch do driver nativo
- Adicionar timeout explícito dentro das chamadas HTTP ao Earth Engine
  (exigiria alterar código vendorizado de terceiros; watchdog externo é
  a mitigação adotada nesta fase)
- Otimização de concorrência do gunicorn (ex.: migrar de `gthread` para
  `sync`/`gevent`) — candidato a spec futura se hangs persistirem após
  o watchdog

## Critérios de Aceitação

1. **CA-1**: `scripts/touchterrain_watchdog.py` expõe
   `check_health(url: str, timeout: float) -> bool` que retorna `False`
   para uma URL inacessível (ex.: porta fechada) e `True` para uma URL
   que responde 200 dentro do timeout.
2. **CA-2**: `touchterrain_watchdog.py` expõe
   `restart_service(service_name: str) -> bool` que invoca
   `systemctl restart <service_name>` e retorna `True`/`False` conforme
   o código de saída do comando.
3. **CA-3**: a função principal do watchdog, quando `check_health` retorna
   `False`, chama `restart_service` exatamente uma vez (idempotência —
   não reinicia repetidamente em rajada).
4. **CA-4**: `geomaker-touchterrain-watchdog.timer` e `.service` existem
   em `/etc/systemd/system/`, o timer está `enabled` e `active`, e o
   `.service` referencia o script correto com `Type=oneshot`.
5. **CA-5** (regressão R143): o código-fonte de
   `TouchTerrain_app.py::preflight_generator` acessa `basethick` (e as
   demais chaves float-ificadas) via `args.get(k, ...)`, nunca via
   `args[k]` direto, prevenindo `KeyError` caso o campo não seja enviado
   pelo formulário.
6. **CA-6**: após a remediação (restart do serviço), uma requisição real
   a `http://localhost:8081/main` com timeout de 15s completa com sucesso
   (não gera `TimeoutExpired`), confirmando que o hang foi sanado.
7. **CA-7**: o script de watchdog não depende de bibliotecas externas
   além da stdlib do Python (urllib, subprocess) — mantém o utilitário
   leve e sem novas dependências no ambiente de produção.

## Estratégia de Verificação

- TDD: testes em `tests/test_r145_touchterrain_estabilidade.py` escritos
  antes da implementação do watchdog (fase vermelha), depois verde após
  implementar `scripts/touchterrain_watchdog.py` e as units systemd.
- `python3 -m pytest tests/test_r145_touchterrain_estabilidade.py -v`
- Verificação manual: `systemctl status geomaker-touchterrain-watchdog.timer`
- Verificação manual: `curl --max-time 15 http://localhost:8081/main`
  responde com sucesso após a remediação
